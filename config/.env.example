# Copy to `config/.env` and fill in (that file is gitignored).
# Used by `scripts/push_remotes.sh` and `scripts/backup_to_nas.sh`.

# Optional: HTTPS or SSH URL for a second remote named `gitea` (self-hosted).
# GITEA_PUSH_URL=

# NAS backup (see config/update_policy.txt). Defaults match the policy; set to override.
# NAS_BACKUP_HOST=oluwasanmi-fedora-server
# NAS_BACKUP_PATH=/home/muyiwa/PrimaryNAS/DataFolder/PycharmProjects/RustPythonCPPBazel
# NAS_SSH_USER=myuser
